apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: null
  generation: 1
  labels:
    app: nick-app
  name: nick-app
  selfLink: /apis/extensions/v1beta1/namespaces/default/deployments/nick-app
spec:
  progressDeadlineSeconds: 600
  replicas: 3
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: nick-app
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: nick-app
    spec:
      containers:
      - image: nick-app:v2
        imagePullPolicy: IfNotPresent
        name: nick-app
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
status: {}
---
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    app: nick-app
  name: nick-app
  selfLink: /api/v1/namespaces/default/services/nick-app
spec:
  externalTrafficPolicy: Cluster
  ports:
  - nodePort: 32000
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: nick-app
  sessionAffinity: None
  type: LoadBalancer
status:
  loadBalancer: {}
